{
  "name": "ai-usage-guardian-extension",
  "version": "1.0.0",
  "description": "Chrome Extension for tracking AI usage across platforms",
  "main": "dist/background.js",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.1.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.263",
    "typescript": "^5.3.3",
    "vite": "^5.1.4"
  }
}
